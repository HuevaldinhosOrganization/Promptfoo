def get_assert(output, context):
    """
    A simple custom evaluate assertion written in Python.
    Args:
        output: The output string generated by the LLM.
        context: A dictionary containing 'vars' and other context details.
    Returns:
        dict: Containing 'pass' (bool), 'score' (float), and 'reason' (str)
    """
    topic = context['vars'].get('topic', '').lower()
    
    # Check if the output is not empty
    if not output:
        return {'pass': False, 'score': 0.0, 'reason': 'Output is empty'}
    
    # Check if the response contains reasonable length
    if len(output) < 10:
        return {'pass': False, 'score': 0.1, 'reason': 'Output is too short to be an explanation'}
        
    return {'pass': True, 'score': 1.0, 'reason': 'Output passed length checks'}
